project Tabulator is

   for Object_Dir use "obj/";
   for Source_Dirs use (".");

   package Ide is
      for Vcs_Kind use "Git";
      for Documentation_Dir use "docs/";
   end Ide;

   package Builder is
      for Default_Switches ("ada") use ("-s", "-j2");
   end Builder;

end Tabulator;

